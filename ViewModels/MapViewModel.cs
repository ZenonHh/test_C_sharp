using CommunityToolkit.Mvvm.ComponentModel;

namespace DoAnCSharp.ViewModels;

public partial class MapViewModel : ObservableObject
{
    public MapViewModel()
    {
        // Sau này bạn có thể thêm logic xử lý tọa độ bản đồ ở đây
    }
}